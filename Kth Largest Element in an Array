class Solution {
    public int findKthLargest(int[] nums, int k) {
       PriorityQueue<Integer> a = new PriorityQueue<>();
       for(int n:nums)
       {
        a.add(n);
        if(a.size()>k)
        {
            a.poll();
        }

       }
       return a.peek(); 
    }
}
